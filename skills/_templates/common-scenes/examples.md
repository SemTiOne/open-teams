# 通用场景示例模板

- “看 `skills/<project-slug>/bugfix/`，帮我定位一个真实报错，先给根因和最小修复方案。”
- “看 `skills/<project-slug>/new-feature-dev/`，在不破坏现有结构的前提下补一个新能力。”
- “看 `skills/<project-slug>/performance-tuning/`，先找真实瓶颈，再做局部优化。”
